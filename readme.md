Plantilla para bootstrap + scss + gulp
======================================

Para instalar los módulos

```bash
pnpm install
```

Para copiar ficheros en su sitio, compilar scss con gulp y quedarse en espera:

```bash
pnpm start
```